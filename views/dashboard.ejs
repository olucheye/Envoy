<%- include('partials/header'); %>

<h4>Welcome <%= username.username %> </h4>

<div class="row px-4">
    <div class="col-12 col-md-6">
        <div class="col-12">
            <h4 class="my-4 p-3 text-center or"><strong>WISHLIST</strong></h4>
            <div class="items">
                <%# pass in array of items here  %> 
                <%shoppingList.forEach(item=>{  %>
                    <%#console.log(item)%>
                    <li> <%= item.name %>  </li>
                <% })  %>
            </div>

            


        </div>
        <form action="/addItem" method="post">
            <input type="text" name="newItem" id="newItem" class="form-control" placeholder="Enter a new item" required>
            <input type="hidden" name="userid" value="<%=username.id%>" id="userid">
            <input type="submit" value="ADD" class="submitItem my-3 px-5 py-1">
        </form>
    </div>
    
</div>


<%- include('partials/footer'); %> 